fastapi==0.141.1
# Pinned explicitly rather than left to FastAPI's floor (>=0.46.0). Versions below 1.3.1
# carry three HIGH CVEs — CVE-2025-62727, CVE-2026-48818, CVE-2026-54283 — which the
# Trivy gate in CI catches. Pinning here makes the security floor visible instead of
# implicit in a transitive dependency.
starlette==1.6.0
uvicorn[standard]==0.52.3
motor==3.6.0
httpx==0.28.1
pydantic-settings==2.7.0
