# Template only — real values live in .env, which is gitignored.
# Run `make local-secrets` to generate .env with random passwords, then paste your key.

# Generated automatically by scripts/bootstrap-local.sh
MONGO_ROOT_PASSWORD=
MONGO_APP_PASSWORD=

# Free key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=
