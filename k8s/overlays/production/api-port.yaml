# Production listens on 9000. See the note in the staging equivalent for why this is a
# JSON 6902 replace rather than a strategic merge patch.
- op: replace
  path: /spec/template/spec/containers/0/ports/0/containerPort
  value: 9000
